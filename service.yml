apiVersion: v1
kind: Service
metadata:
  name: auto-builder-ms
  annotations:
    cloud.google.com/backend-config: '{"default": "auto-builder-ms"}'
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8000
  selector:
    app: auto-builder-ms